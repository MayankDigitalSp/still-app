import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId:'com.still.wellness', appName:'Still', webDir:'out', server:{androidScheme:'https'} };
export default config;
